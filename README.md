Projeto Biblioteca (Flask)

1. Criar virtualenv:
   python -m venv venv

2. Ativar (PowerShell):
   .\venv\Scripts\Activate.ps1

3. Instalar dependências:
   pip install -r requirements.txt

4. Criar .env local a partir de .env.example e ajustar valores.

5. Inicializar DB e rodar:
   python app.py

